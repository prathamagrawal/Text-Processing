import { useState } from "react";
import './Home.css';
const Home = () => {
    return (
        <>{(
            <div className="home">
                <h1> Welcome to Texty</h1>
            </div>
        )}
        </>
    );
};

export default Home;